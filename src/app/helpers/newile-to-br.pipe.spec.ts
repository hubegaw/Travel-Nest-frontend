import { NewlineToBrPipe } from './newline-to-br.pipe';

describe('NewileToBrPipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineToBrPipe();
    expect(pipe).toBeTruthy();
  });
});
